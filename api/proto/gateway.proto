syntax = "proto3";

package piccologatewaypackage;

service PiccoloGatewayService {
  rpc RequestEvent (EventName) returns (Reply);
}

message EventName {
  bool isEnable = 1;
  string name = 2;
  Target target = 3;
}

enum Target {
  StateManager = 0;
}

message Reply {
  bool isOk = 1;
}
