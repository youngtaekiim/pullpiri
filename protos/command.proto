syntax = "proto3";

package command;

service Command {
  rpc Send (SendRequest) returns (SendReply);
}

message SendRequest {
  string cmd = 1;
}

message SendReply {
  bool ans = 1;
  string desc = 2;
}