struct NodeTimeOffset {
	// deviceId; TSN node
	// offset, ptp offset (ns)

	long deviceId;
	long offset;
};

struct NodeDisconnectionStatus{
	// datatype: boolean
	// type: sensor
	// description: Indicates if network disconnected 
	//              True = disconnected False = Normal Status..

	boolean value;
};

