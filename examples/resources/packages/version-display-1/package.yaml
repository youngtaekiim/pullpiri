apiVersion: v1
kind: Package
metadata:
  label: null
  name: version-display-1
spec:
  pattern:
    - type: plain
  models:
    - name: version-display
      resources:
        volume: vd-volume
        network: vd-network
