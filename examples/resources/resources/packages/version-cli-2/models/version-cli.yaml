apiVersion: v1
kind: Pod
metadata:
  name: version-cli
  labels:
    app: version-cli
spec:
  containers:
    - name: cli-container
      image: localhost/version-cli:2.0
  terminationGracePeriodSeconds: 0
