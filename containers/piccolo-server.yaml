# SPDX-FileCopyrightText: Copyright 2024 LG Electronics Inc.
#
# SPDX-License-Identifier: Apache-2.0

# DO NOT USE this file if you can see this comment.

apiVersion: v1
kind: Pod
metadata:
  name: piccolo-server
spec:
  hostNetwork: true
  hostPid: true
  containers:
  - name: rocksdbservice
    image: pullpiri-rocksdb:latest
    imagePullPolicy: Never
    command: ["rocksdbservice", "--path", "/data", "--addr", "0.0.0.0", "--port", "50051"]
    volumeMounts:
    - name: rocksdb-data
      mountPath: /data
    environment:
    - name: RUST_LOG
      value: "info"
    securityContext:
      runAsUser: 0
      runAsGroup: 0
  - name: apiserver
    image: pullpiri:latest
    imagePullPolicy: Never
    command: ["/piccolo/apiserver"]
    volumeMounts:
    - name: piccolo-yaml
      mountPath: /etc/piccolo/yaml
    - name: config-path
      mountPath: /etc/piccolo/settings.yaml
    environment:
    - name: ROCKSDB_SERVICE_URL
      value: "http://localhost:50051"
  - name: policymanager
    image: pullpiri:latest
    imagePullPolicy: Never
    command: ["/piccolo/policymanager"]
    environment:
    - name: ROCKSDB_SERVICE_URL
      value: "http://localhost:50051"
  - name: monitoringserver
    image: pullpiri:latest
    imagePullPolicy: Never
    command: ["/piccolo/monitoringserver"]
    environment:
    - name: ROCKSDB_SERVICE_URL
      value: "http://localhost:50051"
  - name: settingsservice
    image: pullpiri:latest
    imagePullPolicy: Never
    command: ["/piccolo/settingsservice", "--bind-address=0.0.0.0", "--bind-port=8080", "--log-level=info"]
    volumeMounts:
    - name: piccolo-yaml
      mountPath: /etc/piccolo/yaml
    - name: config-path
      mountPath: /etc/piccolo/settings.yaml
    environment:
    - name: ROCKSDB_SERVICE_URL
      value: "http://localhost:50051"
  volumes:
  - name: piccolo-yaml
    hostPath:
      path: /etc/piccolo/yaml
  - name: config-path
    hostPath:
      path: /etc/containers/systemd/piccolo/settings.yaml
  - name: rocksdb-data
    hostPath:
      path: /tmp/pullpiri_shared_rocksdb
